---
name: tulibs-thesis-docx-checker
description: "ตรวจไฟล์ Word (.docx) วิทยานิพนธ์/สารนิพนธ์/การค้นคว้าอิสระ มหาวิทยาลัยธรรมศาสตร์ เทียบเทมเพลต TULIBS ไทย/อังกฤษ rev.2024 หรือ English Times rev.2023 และตรวจ APA 7 แยกภาษาต่อรายการ แล้วส่งกลับ**ไฟล์รายงาน .docx ไฟล์เดียว** แยก 3 ตาราง Template, คำผิด และ APA7 โดยระบุจุดที่ต้องแก้เป็น**ข้อความจริงในเล่มสำหรับก๊อปไปค้นด้วย Ctrl+F** ไม่ใช่เลขหน้า พร้อมวิธีแก้ทีละขั้นในภาษาที่นักศึกษาทำตามได้ทันที. ใช้เมื่อผู้ใช้ขอตรวจ/recheck ก่อนส่งหรือก่อนแปลง PDF, ตรวจ margin/font/size/TU styles/เลขหน้า/สารบัญ/คำผิด/บรรณานุกรม หรือกล่าวถึง TU thesis Word format/TULIBS DOCX. อ่านรูปแบบจาก OOXML ในไฟล์โดยตรงแบบ**อ่านอย่างเดียว** — ไม่แก้ไข ไม่จัดหน้า ไม่ปักคอมเมนต์ ไม่แตะไฟล์ต้นฉบับหรือโฟลเดอร์ของไฟล์ต้นฉบับ ผลลัพธ์คือไฟล์รายงานแยกใบเดียว; เลขแผ่นได้จากการเรนเดอร์สำเนาถ้าทำได้ ถ้าไม่ได้ก็ยังตรวจได้ครบเพราะรายงานอ้างจุดด้วยข้อความ. ทุก finding ต้องมีข้อความจริงที่ยกมา ค่าที่พบ เกณฑ์ และวิธีแก้. รองรับการแบ่งส่วนนำ เนื้อหา/ภาคผนวก และรายการอ้างอิงสำหรับงานขนานในเล่มหนา."
---

# TULIBS Thesis Checker — .docx → รายงาน .docx

ตรวจไฟล์ **.docx** วิทยานิพนธ์ มธ. แล้วออกรายงาน **3 ตารางแยกกันชัดเจน**:

1. **ตารางที่ 1 — รูปแบบเทมเพลต**: หน้ากระดาษ/ระยะขอบ/ฟอนต์/ขนาด/สไตล์/ระยะห่างบรรทัด/เลขหน้า/โครงสร้าง/สารบัญ
2. **ตารางที่ 2 — คำผิด**: คำสะกดผิด อักขระผิดรูป/แปลกปลอม เว้นวรรคซ้อน และคำซ้ำ
3. **ตารางที่ 3 — การอ้างอิง APA 7**: ความถูกต้องของการ**เขียน**รายการอ้างอิงและการอ้างอิงในเนื้อหา แยกตามภาษาของแต่ละรายการ

**ห้ามรวมสามตาราง** — ทุกตารางเรียง finding ตามแผ่นที่ 1 → แผ่นสุดท้ายก่อนระดับความรุนแรง.

> **ขอบเขต** = มีไฟล์ .docx และรัน Python ได้ → ส่งกลับ **ไฟล์เดียว**: `<ไฟล์>_report.docx` ที่มีครบทั้งจุดที่ต้องแก้ (เป็นข้อความสำหรับค้น) เกณฑ์ วิธีแก้ และค่าที่ถูกต้อง

> ### 🔒 อ่านอย่างเดียว — ข้อห้ามที่ไม่มีข้อยกเว้น
>
> สกิลนี้ **ตรวจแล้วออกรายงาน** เท่านั้น ไฟล์ต้นฉบับต้องมีค่า byte เท่าเดิมทุกประการหลังรันเสร็จ
>
> - ❌ ห้ามแก้ไข จัดรูปแบบ จัดหน้าใหม่ หรือเขียนทับไฟล์ต้นฉบับ
> - ❌ ห้ามปักคอมเมนต์/ไฮไลต์/track changes ลงในเล่ม — แม้ผู้ใช้จะขอ
> - ❌ ห้ามสร้างไฟล์ใด ๆ ในโฟลเดอร์เดียวกับไฟล์ต้นฉบับ (รวมไฟล์ล็อกของ LibreOffice)
> - ✅ ต้องเรนเดอร์จาก **สำเนา** ใน temp dir เสมอ
> - ✅ เขียนได้ที่เดียว: ไฟล์รายงานตาม `-o` และ working dir ที่ผู้ใช้ระบุ
>
> ถ้าผู้ใช้ขอให้แก้ไฟล์ให้ **ให้ปฏิเสธและอธิบายว่าสกิลนี้อ่านอย่างเดียว** แล้วชี้ว่ารายงาน
> ระบุจุดด้วยข้อความสำหรับ Ctrl+F ให้เขาแก้เองใน Word ได้ทันที

## หลักที่ทำให้งานนี้เร็วและแม่นพร้อมกัน

**อะไรที่ตัดสินได้โดยไม่ต้องตีความ ให้สคริปต์ทำ** — ใช้ `check_all.py` โหลด OOXML ครั้งเดียวแล้วรันตัวตรวจสองชุดแบบ deterministic. เวลาแปรตามขนาดและความซับซ้อนของไฟล์; อย่าอ้างเวลาตายตัว. สคริปต์เทียบเลขหน้าสารบัญที่จับคู่ได้, ป้ายรายการในตารางส่วนนำ, ความสม่ำเสมอของการเยื้องหัวข้อ, รูปแบบ DOI/ช่วงตัวเลข, hanging indent และการเรียงภาษาอังกฤษ; งานเชิงความหมายและ Thai collation ยังต้องอ่านเอง.

**เปิดคู่มือเฉพาะหน้าที่ใช้** — คู่มือ APA7 ทั้งไทยและอังกฤษถูกซอยเป็นไฟล์เล็กต่อประเภทแหล่งอ้างอิง. เปิดทั้งเล่มเพื่อตรวจรายการวารสารรายการเดียว = โหลดของเกินจำเป็น 20 เท่า และพอ context เต็ม โมเดลจะเริ่มลืมกฎที่สั่งไว้ตอนต้น ผลจึงแย่ลงด้วย ไม่ใช่แค่ช้าลง.

## กฎความแม่นยำ (บังคับทุกการตรวจ)

1. **ใช้ version guard** — อ่าน `references/source-registry.md`. recheck ปกติใช้ pinned rev.2024/rev.2023 โดยไม่ browse; ถ้าผู้ใช้ขอ “ล่าสุด/current” จึงยืนยัน official template หรือระบุว่า revision ปัจจุบันยังไม่ยืนยัน.
2. **อ่านเกณฑ์รูปแบบจาก OOXML เท่านั้น** — ภาพวัด half-point/twips/ชื่อสไตล์ไม่ได้. **ไม่ต้อง render อะไรทั้งสิ้นในงานตรวจปกติ** ทั้งเอกสารนักศึกษาและตัวรายงาน: เกณฑ์อ่านจากไฟล์ตรง ๆ และรายงานไม่อ้างเลขแผ่นแล้ว
   **อ่านช่องให้ถูกภาษา** — อักษรไทยเป็น complex script ค่าที่คุมจริงอยู่ที่ `w:cs` (ฟอนต์) · `w:szCs` (ขนาด) · `w:bCs` (ตัวหนา) ไม่ใช่ `w:ascii`/`w:sz`/`w:b`. เทมเพลตตั้ง `TU_Chapter` เป็น `<w:b w:val="0"/><w:bCs/>` = ละตินไม่หนา ไทยหนา ซึ่งถูกต้อง — อ่านแค่ `w:b` แล้วสรุปว่า "ไม่หนา" คือฟ้องเทมเพลตของหอสมุดเอง (เคยเป็นบั๊กจริง) ดู `references/docx-spec.md` §3
3. **ห้ามสรุปเรื่องการจัดหน้าจากเลขแผ่นที่เดาได้** — เลขแผ่นจาก `--page-engine markers` มาจาก `w:lastRenderedPageBreak` ซึ่งเป็นแคชจากตอน Word เรนเดอร์ครั้งล่าสุด ไม่ใช่การจัดหน้าปัจจุบัน **ใช้ได้อย่างเดียวคือจัดลำดับแถวในรายงาน** ห้ามเขียนลงในช่องปัญหา และห้ามใช้เป็นฐานของ finding กลุ่มนี้:
   - B1 องค์ประกอบปกอยู่ในหน้าเดียวกันครบ
   - B3 บทคัดย่อ ≤ 1 หน้า · B4 กิตติกรรมประกาศ ≤ 1 หน้า
   - แต่ละบทขึ้นหน้าใหม่ · ตารางตกหน้า
   finding กลุ่มนี้ออกได้**ต่อเมื่อ**มี `pagemap.json` จาก `scripts/page_truth.py` (`page_claims_allowed = true`) ถ้าไม่มี ให้ลงว่า "ตรวจไม่ได้จากไฟล์ ต้องเปิดดูใน Word" ตามกฎข้อ 7
   *เคสจริงที่ทำให้ต้องมีกฎนี้: เล่ม 120 แผ่น ตัวนับเดาได้ 141 แผ่น (นับซ้ำทุก hard break) แล้ว agent รายงานว่า "ปกภาษาอังกฤษถูกแบ่งข้ามแผ่น" ทั้งที่ปกอยู่ครบในแผ่นที่ 2 และรายงานว่าหัวข้อบทคัดย่ออยู่แผ่น 5 ทั้งที่อยู่แผ่น 4*
4. **ทุกจุด "ไม่ผ่าน" ต้องครบ 5 อย่าง**: **ข้อความจริงที่พบ ยกมาในเครื่องหมายคำพูด (นี่คือสิ่งที่ผู้อ่านเอาไปค้นใน Word — สำคัญที่สุด)** · แผ่นที่ (ถ้ารู้ ใช้เป็นตัวช่วยกะตำแหน่ง) · เกณฑ์พร้อมอ้างที่มา · วิธีแก้ · ค่าที่ถูกต้อง. **ไม่ต้องระบุเลขย่อหน้า** — ข้อความที่ยกมาคือสิ่งที่ใช้หาจุดจริงทั้งของคนและของสคริปต์ ส่วนเลขย่อหน้าไม่มีใน Word ให้ดูอยู่แล้ว.
5. **รายงานเฉพาะที่ผิดเกณฑ์จริง** — เทียบ `references/docx-spec.md` + `references/format-rules.json` (ค่าตัวเลขล้วน ใช้ตอนเขียน/แก้กฎ) + `references/style-catalog.md` (format) หรือคู่มือ APA7 (อ้างอิง). spec ไม่ได้กำหนด อย่า flag. ห้ามใช้ความรู้สึกว่า "น่าจะผิด" — false positive แพงกว่าการตรวจไม่เจอ เพราะนักศึกษาเสียเวลาแก้สิ่งที่ถูกอยู่แล้ว **แล้วเลิกเชื่อรายงานทั้งฉบับ** ซึ่งทำให้ finding ที่ถูกต้องพลอยถูกมองข้ามไปด้วย. ถ้าไม่แน่ใจระหว่าง "ผิด" กับ "ตรวจไม่ได้" ให้เลือก "ตรวจไม่ได้" เสมอ. **แก้กฎในสคริปต์เมื่อไร รัน `python3 scripts/run_tests.py` ก่อนใช้จริงเสมอ** — มีสองด่าน: `test_false_positives.py` ยิงกฎทีละข้อด้วยข้อความสังเคราะห์ (จับ regex ที่จับเกิน: ตัวเลขใน URL, ชื่อเมืองที่เป็นชื่อเรื่อง, คำขึ้นต้นประโยคที่ตามด้วยวงเล็บปี) และ `test_official_templates.py` รันตัวตรวจ**ทั้งชุด**กับเทมเพลตทางการใน `fixtures/` แล้วบังคับให้ได้ 0 finding
6. **สงสัยว่ากฎไหนถูก ให้เปิด `fixtures/` หรือ `references/templates/` ไม่ใช่เชื่อความจำ** — `fixtures/` มีไฟล์ .docx ทางการ rev.2024 ไทย/อังกฤษ (ตัดฟอนต์ฝังออกให้เล็กลง ค่ารูปแบบครบเหมือนต้นฉบับ) เปิดอ่าน OOXML ยืนยันค่าได้โดยตรง ส่วน `references/templates/` คือเนื้อหาที่สกัดไว้เป็นข้อความของทั้ง 3 โปรไฟล์ = **แหล่งความจริงของเกณฑ์**. เอกสาร "ตัวอย่างการพิมพ์ส่วนประกอบ" เป็นรุ่นเก่ากว่าและขัดกับเทมเพลตในบางจุด (เช่นมีบรรทัดลิขสิทธิ์ที่เทมเพลตไม่มี) — เมื่อขัดกัน **ยึดเทมเพลต**. **แยกให้ออกระหว่าง "ไฟล์ที่หอสมุดทำ" กับ "ผลงานนักศึกษาที่เอามาดูเป็นตัวอย่าง"** — เฉพาะอย่างแรกเท่านั้นที่ใช้เป็นเกณฑ์ได้. **กฎใดที่ฟ้องไฟล์ที่หอสมุดทำ = กฎนั้นผิด ไม่ใช่ไฟล์ผิด** (`test_official_templates.py` บังคับเงื่อนไขนี้กับเทมเพลตจริงทุกครั้ง — ตอนเพิ่มด่านนี้จับ false positive ที่ค้างอยู่ได้ 5 ชนิด). ค่าที่วัดได้จากตัวอย่าง เช่น บันไดการเยื้องและป้ายรายการในตารางส่วนนำ ถูกบันทึกไว้ใน `references/docx-spec.md` §5.1–5.2 แล้ว
7. **ห้ามเดา ห้ามแต่งข้อมูล** — ตรวจไม่ได้ให้เขียนตรง ๆ ว่า "ตรวจไม่ได้จากไฟล์ ต้องเปิดดูใน Word". ข้อมูลต้นฉบับขาด (ไม่มี DOI/วันที่) ให้บอกว่าขาด.
8. **ตรวจครบก่อนสรุป** — ทุกหน้า (format) และทุกรายการในบรรณานุกรม (APA7).

## เลือกวิธีรัน

| | **โหมดเดี่ยว** | **โหมดขนาน** (agent 3 ตัวขึ้นไป) |
|---|---|---|
| เหมาะกับ | เล่มบาง (< ~60 หน้า **และ** < ~60 รายการอ้างอิง) หรือ harness ไม่มี subagent | เกินเกณฑ์นั้น + harness spawn ขนานได้ (Claude Code / Codex / Cowork) |
| วิธีทำ | ขั้น 1–5 ด้านล่าง | [โหมดขนาน](#โหมดขนาน) แล้วอ่าน `references/agent-contracts.md` |

**จำนวนรายการอ้างอิงสำคัญพอ ๆ กับจำนวนหน้า** — เล่ม 50 หน้าที่มี 150 รายการยังควรใช้โหมดขนาน เพราะ APA7 คิดต้นทุนต่อรายการ (ต้องเปิดคู่มือคนละไฟล์ต่อประเภทแหล่ง) ไม่ใช่ต่อจำนวนหน้า

เกณฑ์การตรวจเหมือนกันทั้งสองโหมด ต่างแค่ว่าใครอ่านส่วนไหน.

## Workflow — 5 ขั้น

### ขั้น 1 — โหลดไฟล์ครั้งเดียว รันตัวตรวจสองชุด

```bash
python3 scripts/check_all.py "<ไฟล์>.docx" \
    --auto-json auto.json --deep-json deep.json
```

ตัวรันเดา profile เองได้ (`thai` / `english` rev.2024 = TH Sarabun New 16pt · `english-times` rev.2023 = Times 12pt + บรรทัด 1.5) และเสร็จในไม่กี่วินาที เพราะ**อ่านทุกอย่างจาก OOXML ในไฟล์โดยตรง ไม่เรียกโปรแกรมอื่นเลย**

เรื่องเลขแผ่น: สคริปต์นับจากมาร์กเกอร์แบ่งหน้าของ Word (`--page-engine markers` เป็นค่าเริ่มต้น) ซึ่งคลาดสะสมได้หลายแผ่นในเล่มหนา **ไม่กระทบรายงาน** เพราะรายงานไม่แสดงเลขแผ่น ใช้แค่จัดลำดับแถว — ความคลาดแบบสะสมไม่ทำให้ลำดับสลับ

**แต่ถ้าจะตรวจเกณฑ์ที่ขึ้นกับการจัดหน้า** (บทคัดย่อ/กิตติกรรมประกาศ ≤ 1 หน้า · องค์ประกอบปกอยู่หน้าเดียวกัน · แต่ละบทขึ้นหน้าใหม่) **ต้องรันขั้นนี้เพิ่มก่อน** ไม่งั้นข้ามเกณฑ์กลุ่มนี้ไปเลยตามกฎข้อ 3:

```bash
python3 scripts/page_truth.py "<ไฟล์>.docx" -o work/pagemap.json
```

- exit 0 = `page_claims_allowed: true` → ใช้เลขแผ่นใน `pagemap.json` ได้ (ของจริงจาก PDF)
- exit 3 = เรนเดอร์ไม่ได้ → **ห้าม**ออก finding กลุ่มนั้น ลงว่า "ตรวจไม่ได้จากไฟล์"

สคริปต์บอกค่า drift ให้ด้วย (marker เดา vs หน้าจริง) วัดกับเล่มจริง 120 แผ่นได้ −5 แผ่น หลังแก้บั๊กนับซ้ำแล้ว — ยืนยันว่าเลข marker เอาไปอ้างในรายงานไม่ได้

> **เจอ error เรื่อง LibreOffice/soffice หรือ sandbox** (เช่น crash report ของ soffice เด้งใน Codex) → อ่าน `references/troubleshooting.md` ไม่ต้องแก้เอง สคริปต์จำและถอยไปใช้ marker ให้แล้ว

### ขั้น 2 — รู้ว่าอะไรตรวจให้แล้ว จะได้ไม่ทำซ้ำ

| สคริปต์ทำให้แล้ว (อย่าไล่อ่านซ้ำ) | ยังต้องอ่านเอง (ขั้น 3) |
|---|---|
| A4 · ระยะขอบทุก section · header/footer | องค์ประกอบหน้าปก/ปกภาษาที่สองครบและเรียงถูก |
| ฟอนต์ effective แยกไทย-ละติน (รวมในตาราง) · ขนาด/ค่า drift ของ named style · ระยะห่างบรรทัด | หน้าอนุมัติ: ใช้แบบเดียวกันทั้งหน้า · ตำแหน่งกรรมการตรงกับที่สอบจริง |
| สไตล์ TU_* ครบ/ไม่เพี้ยน · page-number field | บทคัดย่อ: **เนื้อหา**ในตารางตรงกับหน้าปกไหม · ไม่เกิน 1 หน้า |
| **ป้ายรายการในตารางบทคัดย่อไทย/อังกฤษครบ 6 รายการ · ประธานกรรมการ+คณบดีในหน้าอนุมัติ · ชื่อกรรมการอยู่ในวงเล็บ · ป้ายในประวัติผู้เขียน** | — |
| **สไตล์ของหัวข้อตรงระดับ (X.X→TU_Main Heading_ChapterN · X.X.X→TU_Sub-heading 1 · X.X.X.X→TU_Sub-heading 2)** · **สไตล์หัวข้อใหญ่ที่ใช้ข้ามบท (ก๊อปหัวข้อจากบทก่อนแล้วเลขนับต่อ)** · การเยื้องหัวข้อสม่ำเสมอ · ลำดับชั้นไม่กลับด้าน | — |
| **หน้าปกตรวจสองทิศทาง: บรรทัดบังคับครบไหม (โดย/BY · ข้อความหลักสูตร · มหาวิทยาลัย · ปีการศึกษา) + มีบรรทัดที่เทมเพลตตัดออกแล้วค้างอยู่ไหม (ลิขสิทธิ์/COPYRIGHT)** · ประโยครับรองบนหน้าอนุมัติ | การจัดกึ่งกลางบนปก (OOXML แยกไม่ออกว่าจัดด้วย jc หรือด้วยการเว้นวรรค) |
| **บรรทัดว่างที่เคาะ Enter คั่นกลางย่อหน้าเนื้อความ** · **ย่อหน้าเนื้อความที่จัด justify** | การเว้นบรรทัดบนปก/ก่อนหัวข้อ = การจัดหน้าปกติ ไม่ใช่ข้อผิดพลาด |
| เลขหน้าสารบัญเทียบหน้าจริง **เฉพาะรายการที่จับคู่ชื่อได้** | รายการที่จับคู่ไม่ได้ให้สุ่มยืนยัน · หัวข้อย่อยแบ่งระดับสมเหตุสมผล |
| — | ตารางตกหน้า: ทวนหัวตาราง + (ต่อ)/(Continued) · **caption ตาราง/ภาพ** (สคริปต์ไม่ตรวจแล้ว ดูหมายเหตุใน `check_deep.py`) |
| **เลขบทต่อเนื่อง · แต่ละบทขึ้นหน้าใหม่** | ชื่อบทอังกฤษเป็นตัวพิมพ์ใหญ่ทั้งหมด |
| **คำสำคัญ/Keywords ตัวหนา** | ชื่อผู้เขียนไม่มีคำนำหน้า · ปกไทย พ.ศ. ปกอังกฤษ ค.ศ. |
| ข้อความปก (คำติดกัน/ปีผิดระบบ) · คำสะกดผิดที่ชัดเจน | — |
| **APA7:** hanging indent · `doi:` · จุดปิดท้าย DOI · `Retrieved from` · ที่ตั้งสำนักพิมพ์ · ช่วงตัวเลข hyphen · `pp.`/ปีผิดระบบ · การเรียงภาษาอังกฤษ · in-text ที่ไม่มีในรายการ | **APA7:** ความถูกของแต่ละประเภทแหล่ง · Thai collation · ตัวเอียง · Title/Sentence case · ชื่อผู้แต่งไทย · "และ/และคณะ" |

### ขั้น 3 — เปิดอ่านเอกสารครั้งเดียว ตรวจสองด้านพร้อมกัน

อ่านไฟล์รอบเดียว (Read/python-docx) ไล่ต้นจนจบ ตรวจเฉพาะคอลัมน์ขวาของตารางข้างบน แยกบันทึกเป็น 3 domain:

**A) format → `domain: "template"`** — รายละเอียดครบใน `references/inspection-checklist.md` หมวด B

**B) spelling → `domain: "spelling"`** — ตัวตรวจอัตโนมัติแยกคำผิดทีละจุดอยู่แล้ว; finding ที่คนพบเพิ่มต้องระบุแผ่นที่และข้อความรอบคำ

**C) APA7 → `domain: "apa7"`** — ใช้เนื้อหารายการอ้างอิงที่อ่านมาแล้วในข้อ A ซ้ำ ไล่ทีละรายการ:

1. **แยกภาษาของ "รายการนั้น"** ไม่ใช่ของทั้งเล่ม — เล่มไทยที่อ้างงานฝรั่งต้องใช้กฎอังกฤษกับรายการฝรั่ง
2. **ไทย** → เปิด `references/apa7-tha-parts/00-INDEX.md` แล้วเปิด**เฉพาะไฟล์**ของประเภทนั้น (`ref-01-วารสาร…` ฯลฯ). ต้องค้นข้ามหมวดให้ `grep` ในโฟลเดอร์ `apa7-tha-parts/` เอา
3. **อังกฤษ** → เปิด**เฉพาะไฟล์**ตรงประเภทใน `references/apa7-eng/` (เช่น `Journal-article-references.md`). บรรทัดสุดท้ายของแต่ละไฟล์คือ `เกณฑ์อ้างอิง: APA Publication Manual §…` — ก๊อปเลขมาตรานี้ใส่ช่อง `criterion` ของ finding เพื่อให้ผู้อ่านตรวจสอบย้อนได้ว่ากฎมาจากไหน
4. **กฎไทยที่ต่างจากอังกฤษ**: ชื่อผู้แต่งไม่กลับคำ · "และ"/"และคณะ" แทน "&"/"et al." · block quote เยื้อง 0.8"
5. ถูกอยู่แล้ว → ไม่ต้องใส่ใน findings

รูปแบบ finding: `domain` (`template`/`spelling`/`apa7`) · `severity` · `location` · `category` · `issue` · `criterion` · `fix` · `correct`.

**สิ่งที่ขาดไม่ได้คือ `snippet` — ข้อความจริงในเล่มตรงจุดที่ผิด ก๊อปมาเป๊ะ ๆ** (หรือยกไว้ในเครื่องหมายคำพูดใน `issue`) เพราะรายงานเอาไปแสดงเป็นคำค้นให้ผู้อ่านวางใน Ctrl+F. `location` ใส่แค่คำบอกส่วน (เช่น "หน้าปกใน", "รายการที่ 14") พอ — เลขแผ่น/เลขย่อหน้าไม่ต้องใส่ ใส่มาก็ไม่ถูกแสดง. `make_report.py` ยังรองรับ `detail` แบบเก่าเพื่อ backward compatibility.

### ขั้น 4 — ออกรายงาน .docx แยก 3 ตาราง (บังคับทุก recheck)

```bash
python3 scripts/make_report.py auto.json deep.json apa7.json --docx "<ไฟล์>.docx" -o "<ไฟล์>_report.docx" \
    --title "<ชื่อวิทยานิพนธ์>"
```

**ช่อง "จุดที่ต้องแก้" ในรายงานคือ "คำค้น" ไม่ใช่เลขแผ่น** — รายงานยกข้อความจริงในเล่มมาให้ผู้อ่านก๊อปไปวางใน Ctrl+F ของ Word แล้วกระโดดถึงจุดนั้นได้ทันที เลขแผ่นถูกลดบทบาทเป็นบรรทัดเล็กใต้ข้อความ (เพราะเลื่อนทุกครั้งที่นักศึกษาแก้ไฟล์). ต้องส่ง `--docx "<ไฟล์>.docx"` ไปด้วยเสมอ สคริปต์จะได้ดึงข้อความจริงมาเติมให้ finding ที่ไม่ได้ยกข้อความมาเอง **และตรวจสอบว่าคำค้นทุกอันมีอยู่จริงในเล่ม** — อันที่ค้นไม่พบจะติดธง ⚠ ไว้ในรายงานและพิมพ์จำนวนออกมาตอนรัน (ค้นไม่พบมักแปลว่า finding นั้นเองผิด ให้ตรวจซ้ำก่อนส่ง)

**ทุกแถวต้องตอบให้ได้ว่า "ตรงไหนในเล่มของฉัน"** — ผู้อ่านรายงานคือนักศึกษาที่ส่วนใหญ่ไม่รู้จักระบบสไตล์ของ Word. แถวที่เขียนว่า "แก้ที่สไตล์ TU_Main Heading_Chapter2" โดยไม่บอกว่าสไตล์นั้นคือข้อความบรรทัดไหน = แถวที่แก้ไม่ได้ ต่อให้เนื้อหาถูกต้อง. กฎที่ใช้:

- finding ระดับสไตล์ **ต้องยกข้อความจริงที่ใช้สไตล์นั้นมาด้วยเสมอ** และบอกว่ามีผลกี่จุด — `check_docx.py` นับ `style_usage()` ให้แล้ว
- **สไตล์ที่ไม่มีข้อความไหนใช้ ห้ามแตกเป็นหลายแถว** เพราะแก้แล้วหน้ากระดาษไม่เปลี่ยนอะไรเลย และไม่มีทางชี้จุดได้ — ยุบเป็นแถวเดียวที่บอกตรง ๆ ว่า "ไม่กระทบหน้ากระดาษ ไม่ต้องรีบแก้" (ในเล่มจริง 83% ของ finding ประเภทสไตล์เป็นแบบนี้)
- finding ระดับ section (ระยะขอบ/ขนาดกระดาษ) **ห้ามเขียนว่า "Section 7" หรือ "ส่วนนี้" ลอย ๆ** — Word ไม่แสดงเลข section ที่ไหนเลย ให้ยกข้อความบรรทัดแรกของส่วนนั้นมาเป็นคำค้นแทน (`section_anchors()`)
- เหลือคำว่า "ทั้งเอกสาร" ได้เฉพาะปัญหาที่เป็นทั้งเล่มจริง ๆ (ไม่มีสไตล์เทมเพลต · กระดาษไม่ใช่ A4 · ไม่มีเลขหน้าอัตโนมัติ) และแถวนั้นต้องมีขั้นตอนกดปุ่มครบใน "วิธีแก้"
- ช่อง "วิธีแก้" เขียนเป็นลำดับปุ่มที่กดจริงใน Word และเลี่ยงศัพท์ที่ต้องรู้มาก่อน; ถ้าเสนอทางลัดผ่านสไตล์ ให้แนบทางที่ไม่ต้องใช้สไตล์ไว้ด้วยเสมอ

**สองอย่างที่สคริปต์จัดการให้แล้ว อย่าทำซ้ำเอง**: (1) คำค้นที่ค้นไม่เจอเพราะ Word แบ่ง caption/หัวข้อเป็นหลาย run จะถูกตัดท้ายอัตโนมัติจนเหลือส่วนที่มีจริง (เช่น `ภาพที่ 2.1แผนที่…` → `ภาพที่ 2.1`) และโค้ดของ field (` FORMTEXT `, ` PAGE \* MERGEFORMAT `) ถูกกรองออกจากคำค้นแล้ว — เทมเพลต TULIBS ใช้ form field ทุกช่องที่ให้กรอก ถ้าไม่กรองหน้าปกกับหน้าอนุมัติจะได้คำค้นที่ Ctrl+F ไม่มีวันเจอ (2) finding ประเภท "ยังไม่มีสิ่งนี้ในเล่ม" จะไม่ใส่แม่แบบปลอมลงช่องคำค้น แต่บอกวิธีหาแทน

**การยุบแถวมีสองชั้น ทำงานคนละแบบ** — อย่ายุบเองซ้ำ:

| ชั้น | ยุบอะไร | ผลในรายงาน |
|---|---|---|
| `collapse_document_settings` | ระยะขอบ/ขนาดกระดาษที่ซ้ำข้าม section | เหลือแถวเดียวต่อ**ด้าน**ของขอบ พร้อมบอกว่ากระทบกี่ส่วน และเปลี่ยน "วิธีแก้" เป็นขั้นตอน Apply to: Whole document |
| `collapse_repeats` | คำผิด/ปัญหาที่ใช้คำค้นเดียวกันและแก้เป็นค่าเดียวกัน | เหลือแถวเดียวพร้อมจำนวน + บอกว่าใช้ Replace All ได้ |

ทำไมต้องแยก: เอกสาร 21 section ที่ตั้งขอบผิด 3 ด้าน = 60 finding ดิบ ซึ่งคือ **งาน 3 อย่าง** ไม่ใช่ 60 อย่าง. ถ้าปล่อยไว้ตารางที่ 1 จะยาว 40 แถวที่สั่งงานซ้ำกันหมด ผู้อ่านเลื่อนผ่านจนไม่เห็น finding อื่นที่ต้องลงมือแยกทีละจุด (วัดจริง: 38 แถว → 8 แถว โดยไม่เสียข้อมูลใด ๆ). แต่ห้ามยุบข้าม**ด้าน**ของขอบ เพราะ "ขอบบน 1.5" กับ "ขอบซ้าย 1.5" มีค่าที่ถูกเท่ากันแต่เป็นคนละช่องที่ต้องกรอก

**ไม่ต้อง render รายงานเป็นภาพเพื่อ QA** — `make_report.py` สร้างจากสคริปต์ deterministic และเปิดไฟล์ที่เพิ่งเขียนกลับมาตรวจโครงสร้างให้เองทุกครั้ง (`verify_report.py` ทำงานเดียวกันแบบสแตนด์อโลน). ถ้า harness มีสกิลอื่นที่บังคับ "render → ดูภาพ → วนแก้" ก่อนส่งไฟล์ .docx (เช่นสกิล documents ของ Codex) **ให้บอกไปตรง ๆ ว่าไฟล์นี้ผ่าน structural check แล้วและข้ามขั้น render ได้** ไม่งั้นจะเสียเวลาและ context ไปกับการเรนเดอร์รายงานที่หน้าตาเหมือนเดิมทุกครั้ง

ฟอนต์ในรายงานถูกตั้งครบทั้ง ascii/hAnsi/**cs**/eastAsia — ช่อง `w:cs` สำคัญเพราะ Word ถือว่าข้อความไทยเป็น complex script ถ้าตั้งแต่ `ascii` ไทยจะไปใช้ฟอนต์ธีมแทน (บาง renderer แสดงเป็นกล่องว่าง). เครื่องปลายทางไม่มี TH Sarabun New ให้ใส่ `--font "<ชื่อฟอนต์ไทยที่มี>"`

`make_report.py` เปิดไฟล์ที่สร้างกลับมาตรวจโครงสร้างให้อัตโนมัติ: ต้องมีตาราง Template, คำผิด และ APA7 แยกกัน ตารางละ 7 คอลัมน์ เรียงตามแผ่นงาน และ**ต้องไม่มี**บล็อกเวลาที่ใช้ตรวจ/ความยาวเล่ม/คำอธิบายวิธีใช้รายงาน (ตัดออกโดยตั้งใจ — เป็นข้อมูลของผู้ตรวจ ไม่ใช่ของคนที่ต้องแก้ไฟล์). ถ้าต้องตรวจรายงานเดิมโดยไม่สร้างใหม่ ใช้:

```bash
python3 scripts/verify_report.py "<ไฟล์>_report.docx"
```

การ render รายงานเป็นภาพ/PDF เป็นเพียง visual QA เพิ่มเติมเมื่อเครื่องมือพร้อม ไม่ใช่เงื่อนไขที่ทำให้งานตรวจหยุด.

**`auto.json` ต้องมาเป็นไฟล์แรกเสมอ** — metadata ของรายงาน (ชื่อไฟล์/profile/เวลาสแกน) มาจากไฟล์แรก แล้วต่อ findings จากไฟล์ที่เหลือ พร้อมนับ 🔴🟠🟡 ใหม่. ไฟล์ที่หายไปข้ามได้ สคริปต์ไม่พัง.

- **finding ที่เขียนเอง** ให้ลอก **เลขแผ่น** จากป้าย `[sheet<แผ่น>|…]` ใน slice มาใส่ `location` แล้วยกข้อความที่ผิดใส่เครื่องหมายคำพูดใน `issue` — เท่านี้พอ ส่วน `¶` ในป้ายไม่ต้องสนใจ
- **เลขแผ่นในป้ายห้ามโผล่ในช่อง `issue`** — มันเป็นค่าประมาณ ใส่ได้เฉพาะช่อง `location` ที่รายงานตัดทิ้งอยู่แล้ว. ประโยคแบบ "ข้อความนี้อยู่ท้ายแผ่นที่ 1 ส่วนที่เหลืออยู่แผ่นที่ 2" = ผิดกฎข้อ 3 ต้องมี `pagemap.json` ก่อนจึงเขียนได้
- ถ้ามีเฉพาะผลสคริปต์: `check_docx.py "<ไฟล์>.docx" --report "<ไฟล์>_report.docx"` ก็ได้รายงานเลย

### ขั้น 5 — ตรวจรอบสอง (เฉพาะเมื่อผู้ใช้ขอ "ละเอียดมาก")

ใช้ prompt second-pass ใน `PROMPT.md` — ไล่ความไม่สม่ำเสมอเล็ก ๆ ที่สคริปต์ไม่จับ (bold/ฟอนต์แอบเปลี่ยน, เยื้องไม่เท่า, punctuation ในรายการอ้างอิงไม่สม่ำเสมอ). แต่ละจุด = หนึ่งแถว แยก `domain` ตามเดิม.

## โหมดขนาน

แบ่งตาม **ส่วนของเอกสาร ไม่ใช่หัวข้อที่ตรวจ** — ถ้าแบ่งเป็น "ตัวหนึ่งตรวจ format ตัวหนึ่งตรวจ APA" ทั้งคู่ยังต้องอ่านเล่มเดียวกันทั้งเล่มอยู่ดี context บวมเท่าเดิม แค่ทำงานพร้อมกัน

```bash
python3 scripts/split_docx.py "<ไฟล์>.docx" -o work/
python3 scripts/check_all.py "<ไฟล์>.docx" \
    --auto-json work/auto.json --deep-json work/deep.json
# → เปิด work/manifest.json อ่านคีย์ "agent_plan" แล้ว spawn ทุกตัวในเทิร์นเดียว
#   prompt + สัญญาผลลัพธ์อยู่ใน references/agent-contracts.md — **ห้ามคิดแผนการแบ่งเอง**
python3 scripts/make_report.py work/auto.json work/deep.json work/agent*.json --docx "<ไฟล์>.docx" \
    -o "<ไฟล์>_report.docx" --title "<ชื่อ>"
```

`split_docx.py` ทำสามอย่างที่ทำให้โหมดนี้คุ้ม แล้วเขียนผลไว้ใน `manifest.json` ให้หมด:

1. **ตัดร้อยแก้วออกจากชิ้นงาน** (`--body-mode suspects` เป็นค่าเริ่มต้น) เหลือหัวข้อ/caption/บรรทัดสั้น/หัวตาราง เพราะกฎที่ agent ต้องตรวจไม่ได้ใช้เนื้อความ ส่วนคำผิด/ฟอนต์สคริปต์ตรวจไปแล้ว — วัดกับเล่ม 135 หน้า **ลดข้อความที่ agent อ่านลง 73%** (ต้องการฉบับเต็มใส่ `--body-mode full`)
2. **จัดกลุ่มรายการอ้างอิงตามประเภทแหล่ง** แล้วบอกใน `agent_plan` ว่าก้อนนี้เปิดคู่มือไฟล์ไหน — agent เปิดคู่มือครั้งเดียวต่อก้อน แทนที่จะไล่เดาประเภทแล้วเปิดทีละรายการ (ก้อนที่เล็กกว่า 5 รายการถูกยุบรวมกันเพราะไม่คุ้ม overhead)
3. **กำหนดจำนวน agent ให้พอดีเล่ม** — เล่มบางได้ 3 ตัว เล่มหนาได้ 4–6 ตัว ปรับเพดานด้วย `--max-chars` / `--max-ref-entries` / `--max-ref-agents`

**กติกาที่ทำให้ไม่พัง**

- **agent เขียน JSON ลงไฟล์ แล้วตอบกลับแค่ path + จำนวน finding** — ถ้าตอบ finding ทั้งก้อนเป็นข้อความ context ที่อุตส่าห์แบ่งมาจะไปบวมที่ orchestrator แทน เสียของ
- **agent ไม่คุยกันและไม่อ่านผลของกันและกัน** — จงใจ เพื่อให้ harness ที่ spawn ขนานไม่ได้ รันเรียงลำดับแล้วได้ผลเท่ากันเป๊ะ
- **สร้างรายงานเป็นงานของ orchestrator** — deterministic ล้วน spawn agent มาทำแค่ช้าลง
- **`intext.txt` ไปกับ Agent 3.1 ตัวเดียว** — การจับคู่ in-text ↔ รายการท้ายเล่มต้องเห็นรายการครบทั้งเล่ม ตัวที่เห็นแค่ช่วงของตัวเองจะรายงานผิดว่า "อ้างแล้วไม่มีในรายการ"
- **เช็คก่อนส่ง**: `template` ต้องอยู่ตาราง 1, `spelling` ตาราง 2 และ `apa7` ตาราง 3; แก้ `domain` ใน JSON แล้วรัน `make_report.py` ใหม่ได้โดยไม่ต้อง spawn ซ้ำ

## กรณีพิเศษ

เล่มที่ไม่ใช่เคสมาตรฐาน (ไม่ได้ใช้เทมเพลต · หน้า landscape · ปกสองภาษา · ฟอนต์สมการ · โปรไฟล์ english-times · รายการอ้างอิงผสมภาษา · ไฟล์ PDF) → เปิด `references/edge-cases.md` เฉพาะเมื่อเจอสถานการณ์นั้นจริง

## ไฟล์ในสกิล

| ไฟล์ | ใช้เมื่อ |
|---|---|
| `scripts/check_all.py` | ขั้น 1 — ทางหลัก โหลด DOCX ครั้งเดียวแล้วเขียน `auto.json` + `deep.json` |
| `scripts/check_docx.py` · `scripts/check_deep.py` | ตัวตรวจสองชุดที่ `check_all.py` เรียก (ใช้เดี่ยว ๆ ได้ด้วย `--json`) |
| `scripts/split_docx.py` | โหมดขนาน — ตัดชิ้นงาน + คัดเฉพาะย่อหน้าที่คนต้องดู + จัดกลุ่มรายการอ้างอิง + เขียน `agent_plan` |
| `scripts/make_report.py` | ขั้น 4 — รายงาน 3 ตาราง ระบุจุดด้วยคำค้น (**ต้องใส่ `--docx <ไฟล์ต้นฉบับ>`**) |
| `scripts/verify_report.py` | ตรวจโครงสร้างรายงานเดิมโดยไม่ต้องสร้างใหม่ |
| `scripts/run_tests.py` | **รันหลังแก้กฎใด ๆ เสมอ** — รวมสองด่านทดสอบไว้ในคำสั่งเดียว |
| `scripts/test_false_positives.py` | ด่าน 1 — ยิงกฎทีละข้อด้วยข้อความสังเคราะห์ จับ regex ที่จับเกิน |
| `scripts/test_official_templates.py` | ด่าน 2 — รันตัวตรวจทั้งชุดกับเทมเพลตทางการใน `fixtures/` **ต้องได้ 0 finding** จับความผิดพลาดตอนอ่าน OOXML ที่ด่าน 1 มองไม่เห็น |
| `fixtures/*.docx` | เทมเพลตทางการ rev.2024 ไทย/อังกฤษ (ตัดฟอนต์ฝังออก ค่ารูปแบบครบ) = **เกณฑ์ที่รันทดสอบได้** เปิดอ่าน OOXML ยืนยันค่าเมื่อสงสัย |
| `scripts/find_cover_extras.py` | เสนอ "บรรทัดส่วนเกินบนหน้าปก" จากคลังไฟล์จริงให้คนอนุมัติก่อนเพิ่มเข้า `COVER_DEPRECATED` — การตรวจแบบเทียบเทมเพลตทั้งหน้าทำไม่ได้ (ดู `docx-spec.md` §5.3) |
| `scripts/page_truth.py` | **รันก่อนตรวจเกณฑ์ที่ขึ้นกับการจัดหน้า** — เรนเดอร์ PDF แล้วออก `pagemap.json` + ค่า drift + ธง `page_claims_allowed` |
| `scripts/pagemap_service.py` | **ไม่ใช่ขั้นตอนมาตรฐาน** — worker หาเลขแผ่นจริงเมื่อ sandbox บล็อก soffice (ดู `references/troubleshooting.md`) เรนเดอร์จากสำเนาเช่นกัน |
| `references/docx-spec.md` · `style-catalog.md` | เกณฑ์ format 3 โปรไฟล์ · ค่าสไตล์ TU_* (เปิดเมื่อ finding ต้องอ้างเกณฑ์) |
| `references/inspection-checklist.md` | ขั้น 3A รายการที่ต้องตรวจด้วยตา |
| `references/apa7-tha-parts/00-INDEX.md` · `apa7-eng/*.md` | ขั้น 3B เปิด**เฉพาะไฟล์ตรงประเภท** (โหมดขนานบอกชื่อไฟล์ไว้ใน `agent_plan` ให้แล้ว) |
| `references/agent-contracts.md` | โหมดขนาน — prompt ของ agent + schema JSON |
| `references/troubleshooting.md` · `edge-cases.md` | เปิดเมื่อเจอ error สภาพแวดล้อม / เล่มที่ไม่ใช่เคสมาตรฐาน |
| `references/source-registry.md` | เปิดก่อนอ้างว่าเป็นเกณฑ์ “ล่าสุด” |
| `PROMPT.md` | prompt สำเร็จรูป + ลำดับงานผู้ใช้ |
