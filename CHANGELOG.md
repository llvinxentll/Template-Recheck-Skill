# Changelog

รูปแบบ: [Keep a Changelog](https://keepachangelog.com/) · วันที่แบบ ค.ศ.

## [Unreleased]

### 2026-08-18 — รอบปรับปรุงความแม่นยำและคุณภาพรายงาน

วัดผลกับวิทยานิพนธ์จริง 10 เล่ม และไฟล์ตัวอย่างทางการของหอสมุด 4 ไฟล์

#### Added
- `scripts/test_false_positives.py` — ชุดทดสอบเก็บเคสที่ "ถูกอยู่แล้วแต่เคยถูกฟ้อง"
  และบังคับว่ากฎทุกข้อต้องไม่ฟ้องไฟล์ตัวอย่างทางการ
- ตรวจป้ายรายการในตารางส่วนนำ (บทคัดย่อไทย/อังกฤษ · หน้าอนุมัติ · ประวัติผู้เขียน)
- ตรวจความสม่ำเสมอของบันไดการเยื้องหัวข้อ และลำดับชั้นหัวข้อที่กลับด้าน
- ตรวจบรรทัดบังคับบนหน้าปกไทย/อังกฤษ และประโยครับรองบนหน้าอนุมัติ
  (บรรทัด "ลิขสิทธิ์ของมหาวิทยาลัยธรรมศาสตร์" ขาดใน 8 จาก 9 เล่มที่วัด)
- ตรวจบรรทัดว่างที่เคาะ Enter คั่นกลางย่อหน้าเนื้อความ
- `docx-spec.md` §5.1–5.4 — ค่าที่วัดได้จากไฟล์ตัวอย่างทางการ

#### Changed
- **รายงานชี้จุดได้ทุกแถว** — finding ระดับสไตล์ยกข้อความจริงที่ใช้สไตล์นั้นมาแสดง
  พร้อมจำนวนจุดที่ได้รับผล · finding ระดับ section ยกบรรทัดแรกของส่วนนั้นมาเป็นคำค้น
  แทนคำว่า "Section 7" ที่ Word ไม่แสดงที่ไหนเลย · แถวที่เหลือคำว่า "ทั้งเอกสาร"
  ลดจาก 31 เหลือ 6 และทั้ง 6 เป็นปัญหาทั้งเล่มจริง
- สไตล์ TU_* ที่ค่าไม่ตรงเทมเพลตแต่ **ไม่มีข้อความไหนใช้** ยุบเหลือแถวเดียว
  (เดิมกระจายเป็น 85 จาก 103 แถว ซึ่งแก้แล้วหน้ากระดาษไม่เปลี่ยนอะไร)
- เรียงแถวในรายงานตามลำดับย่อหน้าจริง ไม่ใช่เลขแผ่นที่ยุบรวมกัน
- คำค้นที่ค้นไม่เจอเพราะ Word แบ่ง caption เป็นหลาย run จะถูกตัดท้ายจนเหลือส่วนที่มีจริง
  (แถวติดธง ⚠ ลดจาก 71 เหลือ 1)
- แถวที่ใช้คำค้นและค่าที่ถูกต้องเหมือนกันถูกยุบเหลือแถวเดียวพร้อมบอกจำนวน
- แปลข้อความภาษาเครื่องที่เหลือเป็นภาษาที่ลงมือแก้ได้ (missing TU styles · page size ≠ A4 ·
  page-number field)
- `references/apa7-eng/` 52 ไฟล์ — ตัดขยะจากการ scrape (233 KB → 166 KB) ที่สำคัญคือ
  `opens in new window` 166 จุดที่ปนอยู่ในข้อความ DOI ทำให้ตัวอย่าง DOI ในคู่มือผิดรูป

#### Fixed
- ตัวตรวจ "อ้างในเนื้อหาแต่ไม่มีในรายการอ้างอิง" เคยจับคำสุ่มมาเป็นชื่อผู้แต่ง
  (`Table 3 shows GDP (2019)` → ฟ้อง "Table (2019)") — ลดจาก 25 เหลือ 10 จุด
  และทั้ง 10 ตรวจสอบแล้วว่าเป็นของจริง
- ช่วงเลขหน้าไม่ไปแก้ตัวเลขที่อยู่ใน URL/DOI
- ที่ตั้งสำนักพิมพ์ไม่ฟ้องชื่อเรื่องที่ขึ้นต้นด้วยชื่อเมือง
- `sync_skill.command` ตรวจ SKILL.md ด้วยข้อความที่ไม่มีอยู่จริง จึงแจ้ง "รุ่นเก่า" ทุกครั้ง
- ตัวแยก "ร้อยแก้ว" เช็ค ALL-CAPS โดยไม่กันภาษาไทย ทำให้ย่อหน้าไทยล้วนถูกข้ามทั้งหมด

#### Removed
- **การตรวจตำแหน่ง caption (ตารางเหนือ/ภาพใต้) และ "ภาพ/ตารางที่ไม่มีคำบรรยาย"** —
  ตำแหน่ง caption ไม่ใช่เกณฑ์ตายตัวในเทมเพลต และตัวตรวจแยก caption จริงออกจาก
  ประโยคเนื้อหาที่ขึ้นต้นเหมือนกันไม่ได้ ("ตารางที่ 4.3 พบว่า ผู้ตอบแบบสอบถามส่วนใหญ่…"
  คือเนื้อความ) วัดแล้วสร้าง false positive ~150 แถวจาก 10 เล่ม
- บล็อก "เวลาที่ใช้ในการตรวจสอบ" · "ความยาวเล่ม" · "วิธีใช้รายงานนี้" ออกจากรายงาน —
  เป็นข้อมูลของผู้ตรวจ ไม่ใช่ของคนที่ต้องลงมือแก้ไฟล์

---

## หมายเหตุเรื่องประวัติก่อนหน้านี้

รีโพนี้เริ่มบันทึกประวัติที่สถานะวันที่ 2026-08-18 โฟลเดอร์เดิมไม่ได้อยู่ใต้ git
จึงไม่มีประวัติก่อนหน้าให้ย้อน การแก้ไขตั้งแต่ commit แรกเป็นต้นไปถูกบันทึกครบ
