# CHANGELOG

## 2026-08-19 (2) — บังคับโหมดอ่านอย่างเดียว

**ข้อกำหนดใหม่:** สกิลตรวจ format แล้วออกรายงานเท่านั้น ห้ามแตะไฟล์ต้นฉบับ

- **ลบ `scripts/annotate_docx.py`** — เป็นสคริปต์เดียวที่เขียนคอมเมนต์ลงในเล่ม
- **`SKILL.md`** เพิ่มกล่อง 🔒 อ่านอย่างเดียว: ห้ามแก้ไข/จัดหน้า/ปักคอมเมนต์/สร้างไฟล์
  ในโฟลเดอร์ต้นฉบับ **แม้ผู้ใช้จะขอ** และแก้ description ใน frontmatter ให้ตรง
- **`page_truth.py` และ `pagemap_service.py`** เรนเดอร์จาก **สำเนา** ใน temp dir แล้ว
  เดิมชี้ soffice ไปที่ไฟล์ต้นฉบับโดยตรง ซึ่งทำให้เกิดไฟล์ล็อก `.~lock.<ชื่อ>#`
  ในโฟลเดอร์ของนักศึกษา — ไม่ได้ทำให้ผลตรวจผิด แต่ผิดกฎอ่านอย่างเดียว
- **`agents/openai.yaml`, `check_all.py`, `make_report.py`** ตัดการอ้างถึง annotate ออก

### ด่านทดสอบใหม่ — `scripts/test_read_only.py`

`run_tests.py` เพิ่มเป็น 3 ด่าน ด่านใหม่รัน `check_all` → `page_truth` → `make_report`
กับสำเนาเทมเพลตทางการ แล้วยืนยัน sha256 ของต้นฉบับเท่าเดิมและไม่มีไฟล์งอกในโฟลเดอร์
ทดสอบกับเล่มจริง 120 แผ่นแล้วผ่านทั้งสองเงื่อนไข

## 2026-08-19 — แก้เลขแผ่นเพี้ยนและกันไม่ให้เลขที่เดาได้กลายเป็นข้อสรุป

**อาการที่ทำให้ต้องแก้:** รายงานฟ้อง "ปกภาษาอังกฤษถูกแบ่งข้ามแผ่น" และ
"หัวข้อบทคัดย่ออยู่แผ่นที่ 5" ทั้งที่ต้นฉบับถูกต้อง — ปกอยู่ครบในแผ่นที่ 2
และหัวข้อบทคัดย่ออยู่แผ่นที่ 4

### บั๊กในโค้ด (`scripts/check_docx.py` → `build_page_index`)

1. **นับ marker ซ้ำ** — การขึ้นหน้าใหม่หนึ่งครั้งถูกบันทึกด้วย marker สองตัวเสมอ
   (`w:br type="page"` ที่ผู้เขียนกด + `w:lastRenderedPageBreak` ที่ Word แคชไว้)
   โค้ดเดิมนับทั้งคู่ → บวกเกินครั้งละ 1 แผ่น คลาดสะสมทั้งเล่ม
   แก้เป็น: marker ที่ไม่มีข้อความคั่น = การขึ้นหน้าครั้งเดียวกัน นับครั้งเดียว

2. **ย่อหน้าที่ขึ้นต้นด้วย page break ถูกจัดเข้าหน้าเก่า** — โค้ดเดิมบันทึกเลขหน้า
   ก่อนสแกน break ข้างใน **นี่คือสาเหตุตรงของ false positive เรื่องหน้าปก**
   แก้เป็น: ดูจากตำแหน่งข้อความตัวแรกของย่อหน้า

วัดกับเล่มจริง 120 แผ่น: ตัวนับเดิมได้ 140 แผ่น (ปก = แผ่น 1, บทคัดย่อ = แผ่น 5,
กิตติกรรมประกาศ = แผ่น 10) หลังแก้ได้ 115 แผ่น และทั้งสามจุดตรงกับหน้าจริง

### กฎใหม่ (ป้องกันปัญหาเดิมย้อนกลับมา)

- `SKILL.md` **กฎความแม่นยำข้อ 3** — ห้ามใช้เลขแผ่นจาก marker สรุปเรื่องการจัดหน้า
  ใช้ได้อย่างเดียวคือเรียงลำดับแถวในรายงาน (เลขข้อ 3–7 เดิมเลื่อนเป็น 4–8)
- `references/inspection-checklist.md` — ป้าย 🅿️ กำกับ B1 ปกอยู่หน้าเดียว ·
  B3 บทคัดย่อ ≤ 1 หน้า · B4 กิตติกรรมประกาศ ≤ 1 หน้า
- `references/agent-contracts.md` — ห้าม agent เขียนเลขแผ่นลงช่อง `issue`
  และห้ามออก finding กลุ่ม 🅿️ เว้นแต่ orchestrator ส่ง `pagemap.json` มาให้
- `scripts/split_docx.py` — เตือนเรื่องนี้ในหัวไฟล์ slice ทุกชิ้น
- `agents/openai.yaml` — เลิกสั่งให้ "map every correction to its physical sheet"
  เปลี่ยนเป็นให้ยึดข้อความที่ค้นด้วย Ctrl+F ได้

### สคริปต์ใหม่ — `scripts/page_truth.py`

เกณฑ์กลุ่ม 🅿️ ยังต้องตรวจ ไม่ใช่ทิ้ง สคริปต์นี้เรนเดอร์ PDF จริงแล้วออก
`pagemap.json` (เลขแผ่นของจริง) + ค่า drift + ธง `page_claims_allowed`

```bash
python3 scripts/page_truth.py "<ไฟล์>.docx" -o work/pagemap.json
```

- exit 0 → ตรวจเกณฑ์ 🅿️ ได้
- exit 3 → เรนเดอร์ไม่ได้ ให้ข้ามเกณฑ์ 🅿️ แล้วลงว่า "ตรวจไม่ได้จากไฟล์"

**ยังเหลือ drift −5 แผ่นที่ท้ายเล่ม** เพราะ Word ไม่ได้บันทึก marker ครบทุกหน้า
นี่คือเหตุผลว่าทำไมกฎข้อ 3 ต้องมี ไม่ใช่แก้โค้ดแล้วจบ

### หมายเหตุความแม่นยำ

LibreOffice กับ Word แบ่งบรรทัดภาษาไทยไม่เหมือนกัน 100% เคสก้ำกึ่งอย่าง
"บทคัดย่อเกิน 1 หน้านิดเดียว" ยังพลิกได้ — finding กลุ่ม 🅿️ ควรออกเป็น
🟠 ควรแก้ พร้อมบอกให้เปิด Word ยืนยัน ไม่ใช่ 🔴 ต้องแก้

### ทดสอบแล้ว

`python3 scripts/run_tests.py` ผ่านทั้งสองด่าน — เทมเพลตทางการ rev.2024
ไทย/อังกฤษ ยังได้ 0 finding
