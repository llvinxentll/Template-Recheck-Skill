# PROMPT สำเร็จรูป — ตรวจ Template Format + APA 7 วิทยานิพนธ์ (.docx)

ใช้ prompt เหล่านี้กับ Claude/Codex ที่มี skill `tulibs-thesis-docx-checker` ติดตั้ง
แนบไฟล์งานนักศึกษา (.docx) + (ถ้ามี) คู่มือการพิมพ์วิทยานิพนธ์

---

## PROMPT โหมดขนาน (เล่มหนา / harness ที่ spawn subagent ได้ — Codex, Claude Code, Cowork)

เร็วกว่าและ context ของแต่ละ agent ไม่บวม เพราะแต่ละตัวอ่านคนละส่วนของเล่ม

```
ตรวจไฟล์วิทยานิพนธ์ .docx ที่แนบ แบบขนานด้วย subagent (จำนวนตัวดูจาก agent_plan ที่สคริปต์คำนวณให้)

1. ตัดชิ้นงานก่อน:  python3 scripts/split_docx.py "<ไฟล์>.docx" -o work/
   ซอย body และรายการอ้างอิงให้เองอัตโนมัติ แล้วเขียน "agent_plan" ลง work/manifest.json
   บอกครบว่าต้อง spawn กี่ตัว ตัวไหนอ่านไฟล์อะไร เขียนผลลงไฟล์ไหน — ห้ามคิดแผนเอง
2. รันตัวตรวจอัตโนมัติเองแบบโหลด DOCX ครั้งเดียว ไม่ต้อง spawn:
   python3 scripts/check_all.py "<ไฟล์>.docx" \
       --auto-json work/auto.json --deep-json work/deep.json
3. อ่าน references/agent-contracts.md แล้ว spawn agent "ทุกตัวใน agent_plan ในเทิร์นเดียวกัน":
     Agent 1    → frontmatter.txt                    → agent1-frontmatter.json
     Agent 2.N  → body-N.txt (ก้อนท้ายพ่วง backmatter) → agent2-body-N.json
     Agent 3.N  → references-N.txt (ตัวแรกพ่วง intext) → agent3-apa7-N.json
   ใช้ prompt ตามที่เขียนไว้ในไฟล์นั้นเป๊ะ ๆ แทนค่า <SKILL> <WORK> <PROFILE> <INPUTS> <OUT>
   ให้ครบ (profile และ path ทั้งหมดอยู่ใน work/manifest.json)
4. agent ต้องเขียน JSON ลงไฟล์ แล้วตอบกลับแค่ path + จำนวน finding
   ห้ามตอบ finding ทั้งก้อนกลับมาเป็นข้อความ (จะไปบวม context ของ orchestrator แทน)
5. รวมผลเอง ไม่ต้อง spawn — auto.json ต้องเป็นไฟล์แรกเสมอ:
   python3 scripts/make_report.py work/auto.json work/deep.json work/agent*.json --docx "<ไฟล์>.docx" \
       -o "<ไฟล์>_report.docx" --title "<ชื่อวิทยานิพนธ์>"
6. ดูบรรทัดสรุปที่ make_report พิมพ์ออกมา: "ยืนยันว่าค้นเจอจริง X · ค้นไม่พบ Y"
   ถ้า Y สูงผิดปกติแปลว่า agent ไม่ได้ยกข้อความจริงในเล่มมาใส่ (ยกข้อความที่แก้แล้วมาแทน)
   ให้สั่งแก้ JSON แล้วรัน make_report ใหม่ (ไม่ต้อง spawn agent ซ้ำ)
7. เปิดรายงานเช็คว่า template อยู่ตาราง 1, spelling อยู่ตาราง 2 และ Agent 3/apa7 อยู่ตาราง 3
   ถ้าหลุดตารางให้แก้ domain ใน JSON แล้วรัน make_report ใหม่
8. ส่งกลับผู้ใช้ไฟล์เดียว: <ไฟล์>_report.docx (ทุกจุดที่ต้องแก้ระบุด้วยข้อความสำหรับค้นใน Word)

ถ้า harness spawn ขนานไม่ได้ ให้รัน prompt ทุกตัวเรียงตามลำดับ — ผลเท่ากันเป๊ะ
เพราะ agent ไม่ได้คุยกันและไม่ได้อ่านผลของกันและกัน
```

---

## PROMPT หลัก (ตรวจรอบแรก — โหมดเดี่ยว)

```
โปรดตรวจไฟล์วิทยานิพนธ์ .docx ที่แนบ 2 มิติ:
  (1) รูปแบบตามเทมเพลตมาตรฐาน TULIBS (ระบุ profile: thai / english / english-times
      ถ้าทราบ มิฉะนั้นให้ตรวจจับเอง)
  (2) ความถูกต้องของการอ้างอิง APA 7 ในรายการอ้างอิง/บรรณานุกรมและการอ้างอิงในเนื้อหา

ทำตามนี้:
1. อ่าน references/source-registry.md; ถ้าขอเกณฑ์ล่าสุดให้ยืนยัน official template ก่อน แล้วโหลด DOCX ครั้งเดียวรันตัวตรวจสองชุด:
     python3 scripts/check_all.py "<ไฟล์>.docx" \
       --auto-json auto.json --deep-json deep.json
   ดูตาราง 'อะไรตรวจให้แล้ว' ในขั้น 2 ของ SKILL.md ก่อนไล่อ่านเอง จะได้ไม่ทำซ้ำ
   แล้วจดบรรทัด "Section pages" ที่สคริปต์พิมพ์ออกมาไว้ใช้อ้างแผ่นงานในขั้นถัดไป
2. เปิดไฟล์อ่านครั้งเดียว (Read/python-docx) — ใช้ผ่านรอบเดียวตรวจทั้งสองด้าน:
   ก) format ด้วยตา เทียบ references/inspection-checklist.md
   ข) รายการอ้างอิง/บรรณานุกรม (เนื้อหาเดียวกับที่อ่านในข้อ ก) แยกภาษาต่อรายการ:
      ไทย → references/apa7-tha-parts/00-INDEX.md แล้วเปิดเฉพาะไฟล์ ref-NN ของประเภทนั้น
      อังกฤษ → เปิดเฉพาะไฟล์ตรงประเภทใน references/apa7-eng/*.md (อย่าเปิดทั้งโฟลเดอร์)
3. ตรวจทุกจุดตั้งแต่ต้นจนจบ ทั้ง format ทุกหน้า และ APA7 ทุกรายการ ห้ามข้าม
   ห้ามสรุปว่า "ดูดีแล้ว" ลอย ๆ
4. ทุกข้อที่ไม่ผ่านต้องระบุ: **ข้อความจริงในเล่มที่ยกมาในเครื่องหมายคำพูด** (รายงานเอาไปทำเป็น
   คำค้นให้ผู้อ่านวางใน Ctrl+F ของ Word — ต้องเป็นข้อความที่มีอยู่จริง ห้ามยกข้อความที่แก้แล้ว)
   + แผ่นที่ถ้ารู้ (ไม่ต้องใส่เลขย่อหน้า),
   ค่า/รายการที่พบจริง, ค่าเกณฑ์/กฎ APA7 ที่ใช้, วิธีแก้ที่เป็นรูปธรรม
5. อะไรตรวจจากไฟล์ไม่ได้ ให้เขียน "ตรวจไม่ได้จากไฟล์นี้" — ห้ามเดา ห้ามแต่งข้อมูล
   (ห้ามแต่ง DOI/หน้า/วันที่ที่ไม่มีในต้นฉบับ)
6. อ่าน format จาก python-docx/XML เท่านั้น ห้าม render เป็นภาพ/PDF เพื่อตัดสินฟอนต์/ขนาด/style
   เลขแผ่นงานมาจากมาร์กเกอร์ของ Word ในไฟล์ รายงานจะกำกับ ~ ให้เอง — ห้ามลบ ห้ามเดาเลขแผ่น
7. ***ออกรายงานเป็นไฟล์ .docx ครั้งเดียว โดยแยกผลตรวจเป็น 3 ตาราง***
   ตารางที่ 1 = รูปแบบเทมเพลต, ตารางที่ 2 = คำผิด, ตารางที่ 3 = APA 7:
     # เขียนผลตรวจ APA7 เป็น apa7.json เอง (finding แต่ละรายการใส่ domain: "apa7")
     # - ใส่แผ่นที่ใน location ของ APA7 ด้วย (ดูจากบรรทัด "Section pages" ในข้อ 1)
     # - แยก criterion (กฎ/ที่มา) ออกจาก fix (ขั้นตอนแก้) ให้ชัด
     # - เขียนตัวอย่างที่แก้แล้วลงฟิลด์ correct และครอบชื่อวารสาร/หนังสือด้วย *...* (จะเอียงในตาราง)
     python3 scripts/make_report.py auto.json deep.json apa7.json --docx "<ไฟล์>.docx" -o "<ไฟล์>_report.docx" \
         --title "ชื่อวิทยานิพนธ์"
   คำสั่งจะตรวจโครงสร้างรายงานที่สร้างเสร็จให้อัตโนมัติ
8. ทุก finding ที่เขียนเองต้องยกข้อความจริงที่ผิดใส่เครื่องหมายคำพูดใน issue (หรือใส่ฟิลด์
   snippet) — รายงานเอาไปแสดงเป็นคำค้นให้ผู้อ่านวางใน Ctrl+F และตรวจให้ด้วยว่ามีจริงในเล่ม
   ดูบรรทัดสรุป "ค้นไม่พบ Y รายการ" ถ้า Y สูงให้กลับไปแก้ข้อความที่ยกมาแล้วรันใหม่
9. ส่งกลับผู้ใช้ไฟล์เดียว: <ไฟล์>_report.docx — visual render เป็น optional QA เท่านั้น
```

> **ไม่ต้องติดตั้งหรือเปิดโปรแกรมอะไรเพิ่ม** — สกิลนี้อ่านทุกอย่างจากไฟล์ .docx โดยตรง
> **ห้ามสั่ง `soffice` / `libreoffice` เองเด็ดขาด** (บน macOS จะถูก sandbox ฆ่าด้วย SIGABRT ทุกครั้ง
> แล้วทำให้เข้าใจผิดว่าไฟล์นักศึกษาเสีย) — สกิลนี้ไม่ต้องใช้มันอยู่แล้ว
> ถ้าสคริปต์เตือนว่าไฟล์ไม่มีมาร์กเกอร์แบ่งหน้า ให้บอกผู้ใช้แค่ว่า
> "เปิดไฟล์ใน Word แล้วกด Save ทับหนึ่งครั้ง แล้วส่งมาใหม่" — ห้ามให้ผู้ใช้ไปยุ่งกับ Terminal

> **ไฟล์ PDF**: สกิลนี้ตรวจ .docx เท่านั้น (อ่านค่าจริงในไฟล์ได้ตรงกว่ามาก) — ถ้ามีแต่ PDF ให้ขอไฟล์ Word
> ถ้ามีผลตรวจ PDF เป็น JSON อยู่แล้ว ป้อนเข้า `make_report.py pdf.json apa7.json -o report.docx`
> ได้รายงานแยก 3 ตารางเหมือนกัน (finding จาก PDF ถือเป็น domain "template" อัตโนมัติ)

### รูปแบบผลลัพธ์ (บังคับ) — 3 ตารางแยกกันและเรียงตามแผ่นงาน

**ตารางที่ 1 — Template Format**
```
| ลำดับ | แผ่นที่ + ตำแหน่ง | ระดับ | เกณฑ์ตามคู่มือ TU | ปัญหาที่พบ | วิธีแก้ | ตัวอย่างที่ถูก |
|------|--------------------|------|------------------|-----------|--------|---------------|
```

**ตารางที่ 2 — Spelling**
```
| ลำดับ | แผ่นที่ + ตำแหน่ง | ระดับ | คำ/อักขระที่ผิด | เกณฑ์ | วิธีแก้ | ค่าที่ถูกต้อง |
```

**ตารางที่ 3 — APA 7 Reference Check**
```
| ลำดับ | แผ่นที่ + รายการ | ระดับ | กฎ APA7/TULIBS ที่ใช้ | ต้นฉบับที่ผิด | แก้ไขเป็น |
```

ระดับความรุนแรง: 🔴 Critical · 🟠 Major · 🟡 Minor · 🟢 Suggestion

ถ้าหมวดใดไม่พบปัญหา เขียน "ยังไม่พบข้อผิดพลาดจากผลที่ป้อน — ต้องยืนยัน checklist ก่อนส่ง"
(แยกต่อตาราง ไม่ใช่รวมกัน; ห้ามสรุปว่า “ผ่าน” จากผลอัตโนมัติอย่างเดียว)

### หลังตารางแต่ละตาราง ให้สรุปแยกกัน
- A. จำนวนปัญหาทั้งหมดของตารางนั้น
- B. จำนวน Critical / C. Major / D. Minor ของตารางนั้น
- E. สถานะตามระดับ finding: ยังไม่พบ / พบจุดเล็กน้อย / ควรแก้จุดสำคัญ / ต้องแก้ก่อนส่ง
  ห้ามสร้างเปอร์เซ็นต์เมื่อไม่มีตัวหารหรือเกณฑ์ coverage ที่วัดได้

### ปิดท้าย: checklist สิ่งที่ต้องทำ (เรียงความสำคัญมาก→น้อย ต่อตาราง)
```
□ ต้องแก้ก่อนส่ง
□ ควรแก้
□ แก้เพิ่มได้ (optional)
```

---

## PROMPT เสริม (ตรวจรอบสอง — ละเอียดมาก)

```
ทำการตรวจรอบสอง (second pass)
ละทิ้งผลรอบแรก อ่านทั้งเอกสารใหม่ทั้งหมด (ทั้ง format และรายการอ้างอิง)

ด้าน Template Format โฟกัสความไม่สม่ำเสมอเล็ก ๆ ที่คนมักมองข้าม:
• เว้นวรรคเกิน 1 เคาะ
• bold ไม่สม่ำเสมอ
• ระยะห่างหัวข้อไม่เท่ากัน
• ขนาดฟอนต์แอบต่างในบางย่อหน้า
• line spacing แอบเปลี่ยน
• caption ตาราง/ภาพไม่สม่ำเสมอ
• การเยื้องไม่เท่ากัน
• page break ผิด / heading ลอยท้ายหน้า / ย่อหน้ากำพร้า

ด้าน APA 7 โฟกัสความไม่สม่ำเสมอระหว่างรายการ:
• punctuation ไม่สม่ำเสมอระหว่างรายการ (จุด/คอมม่า/วงเล็บ)
• DOI/URL รูปแบบไม่ตรงกันระหว่างรายการ (บางรายการมี https://doi.org/ บางรายการไม่มี)
• ปีพิมพ์ผสม พ.ศ./ค.ศ. ปนกันในรายการภาษาเดียวกัน
• ตัวเอียงไม่สม่ำเสมอ (บางรายการเอียงชื่อวารสาร บางรายการไม่เอียง)
• การอ้างอิงในเนื้อหาไม่ตรงกับรายการท้ายเล่ม (ขาด/เกิน)

รายงานทุกจุด ห้ามรวบจุดที่คล้ายกัน แต่ละจุด = หนึ่งแถวในตารางของ domain นั้น
(template ตาราง 1, spelling ตาราง 2, apa7 ตาราง 3 — ห้ามปนกันและเรียงแผ่น 1 → N)
```

---

## หลักการยึด (สำคัญ)
- ตรวจทุกหน้า ทุกจุด (format) และทุกรายการอ้างอิง (APA7) อย่าเพิ่งเชื่อว่าถูกจนกว่าจะยืนยันได้
- ถ้ายืนยันจากไฟล์ไม่ได้ → "ตรวจไม่ได้จากไฟล์นี้"
- เวลาแนะนำการแก้ ให้แสดงรูปแบบที่ถูกต้องเป๊ะ ๆ และอ้างกฎจากคู่มือถ้ามี
- ห้ามเดา ห้ามแต่งข้อมูลอ้างอิง (DOI/หน้า/วันที่)
- **รายงานต้องแยกตาราง Template, คำผิด และ APA7 เสมอ และไม่มีบล็อกเวลาที่ใช้ตรวจ/ความยาวเล่ม/วิธีใช้รายงาน**

---

# ลำดับงานสำหรับผู้ใช้ (checklist)

คู่มือลำดับงานสำหรับผู้ใช้ (บรรณารักษ์/อาจารย์/เจ้าหน้าที่) เวลาให้ AI ตรวจไฟล์ .docx
ด้วยสกิล `tulibs-thesis-docx-checker` — ตรวจทั้งรูปแบบเทมเพลตและ APA 7 พร้อมกัน
แล้วได้ **ไฟล์รายงาน .docx** ส่งกลับนักศึกษา

## STEP 0 — เตรียม
- [ ] มีไฟล์งานนักศึกษาเป็น `.docx` (ถ้าเป็น .doc/Google Docs ให้ Save as .docx ก่อน)
- [ ] รู้ว่าเล่มใช้เทมเพลตไหน: ไทย rev.2024 / อังกฤษ rev.2024 / อังกฤษ Times rev.2023
- [ ] ถ้าขอตรวจตาม “เกณฑ์ล่าสุด” ให้ยืนยัน official template ตาม `references/source-registry.md`; มิฉะนั้นระบุว่าใช้ pinned ruleset

## STEP 1 — เปิดสกิลแล้วสั่งตรวจ
- [ ] แนบไฟล์ .docx เข้าห้องแชท
- [ ] เลือกโหมด:
  - **เล่มบาง (< ~60 หน้า)** → วาง **PROMPT หลัก** จาก `PROMPT.md` (โหมดเดี่ยว, STEP 2–5)
  - **เล่มหนา + เครื่องมือ spawn subagent ได้ (Codex/Claude Code/Cowork)** → วาง **PROMPT โหมดขนาน**
    AI จะตัดเล่มด้วย `split_docx.py` แล้วให้ agent ตรวจพร้อมกัน (ส่วนนำ/เนื้อหา/APA7) — จำนวน agent ปรับตามความหนาเล่มและจำนวนรายการอ้างอิงเอง (เล่มบาง 3 ตัว เล่มหนา 6–9 ตัว)
    เร็วกว่ามาก และรายงานปลายทางหน้าตาเหมือนกันทุกประการ
- [ ] ระบุ profile ถ้าทราบ (เช่น "ใช้ profile english-times")

## STEP 2 — AI โหลด DOCX ครั้งเดียวและรันตรวจอัตโนมัติ
- [ ] AI รัน `scripts/check_all.py "<ไฟล์>.docx"` → เขียน `auto.json` และ `deep.json` (ไม่ต้องติดตั้งอะไรเพิ่ม ไม่ต้องใช้ LibreOffice); เลขแผ่นมาจากมาร์กเกอร์ของ Word และจะมี `~` กำกับ — ห้ามลบ `~` และห้ามเดาเลขแผ่น. ถ้าไฟล์ไม่มีมาร์กเกอร์ ให้บอกผู้ใช้ว่า "เปิดใน Word แล้วกด Save ทับหนึ่งครั้ง แล้วส่งใหม่"
      และ `deep.json` (เลขหน้าสารบัญที่จับคู่ชื่อได้, caption ตาราง/ภาพในเนื้อหา,
      เลขบทต่อเนื่อง+ขึ้นหน้าใหม่, คำสำคัญตัวหนา และ APA7 เชิงกลไก (doi:/Retrieved from/ช่วงหน้า/
      pp. ในรายการไทย/ปี ค.ศ. ในรายการไทย/hanging indent/การเรียงอังกฤษ/in-text ที่ไม่มีในรายการ))
- [ ] ตรวจว่า profile ที่ตรวจจับถูกต้อง (ถ้าผิดให้สั่งใหม่ด้วย `--profile`)

## STEP 3–4 — AI เปิดไฟล์อ่านครั้งเดียว ตรวจ format ด้วยตา + APA7 พร้อมกัน
(ประหยัดเวลา ไม่เปิดไฟล์ซ้ำสองรอบ)
- [ ] Template: ปก / หน้าอนุมัติ / บทคัดย่อ / สารบัญ / caption ตาราง-ภาพ / รูปแบบรายการอ้างอิง
- [ ] เลขหน้าสารบัญที่สคริปต์จับคู่ชื่อไม่ได้หรือไฟล์ไม่มี Word page markers ให้สุ่มยืนยันเอง
- [ ] APA7: แยกภาษาแต่ละรายการ — ไทยเริ่มที่ `references/apa7-tha-parts/00-INDEX.md`
      อังกฤษที่ `references/apa7-eng/*.md` — **เปิดเฉพาะไฟล์ตรงประเภท ไม่เปิดทั้งโฟลเดอร์**
- [ ] ตรวจการอ้างอิงในเนื้อหา (in-text) ให้ตรงกับรายการท้ายเล่ม

## STEP 5 — ออกรายงานการแก้ไขเป็นไฟล์ .docx ครั้งเดียว (แยก 3 ตาราง)
- [ ] AI รวม `auto.json` (จาก STEP 2) + `apa7.json` (เขียนเองจาก STEP 3–4, domain: "apa7")
      + `deep.json` แล้วสร้างรายงานคำสั่งเดียว:
      `python3 scripts/make_report.py auto.json deep.json apa7.json --docx "<ไฟล์>.docx" -o "<ไฟล์>_report.docx"`
- [ ] ยืนยันว่าคำสั่งจบโดยไม่มี structural error; ตัวสร้างรายงานตรวจตารางที่ 1 Template, ตารางที่ 2 คำผิด และตารางที่ 3 APA7 ให้อัตโนมัติ
- [ ] ถ้าตรวจรายงานเดิม: `python3 scripts/verify_report.py "<ไฟล์>_report.docx"`
- [ ] ดูบรรทัดสรุปของ `make_report.py`: "ยืนยันว่าค้นเจอจริง X · ค้นไม่พบ Y" — ถ้า Y สูงผิดปกติแปลว่า finding ไม่ได้ยกข้อความจริงในเล่มมาใส่ ให้แก้แล้วรันใหม่
- [ ] เปิดรายงานดูเพิ่มเติมเมื่อเครื่องมือพร้อม; การ render เป็นภาพ/PDF ไม่ใช่เงื่อนไขบังคับ
- [ ] ตรวจว่า**ตารางไม่ได้ปนกัน** — `template`/`spelling`/`apa7` ต้องอยู่ตาราง 1/2/3 ตามลำดับ
- [ ] ตรวจว่าช่อง "จุดที่ต้องแก้" ทุกแถวมีคำค้น 🔎 หรือเขียนว่า "ทั้งเอกสาร" — ต้องไม่มีเลขแผ่นโผล่ (verify_report.py เช็คให้อัตโนมัติ)
- [ ] ตรวจว่ารายงาน **ไม่มี** บล็อกเวลาที่ใช้ตรวจ/ความยาวเล่ม/วิธีใช้รายงาน

## STEP 6 — ตรวจรอบสอง (ข้ามได้ถ้าไม่ขอ — ทำเฉพาะเมื่อต้องการละเอียดมากขึ้น)
- [ ] วาง **PROMPT เสริม (second pass)** จาก `PROMPT.md`

## STEP 7 — ส่งผลกลับนักศึกษา
- [ ] ส่ง **ไฟล์เดียว**: `_report.docx` — ทุกแถวมีข้อความสำหรับก๊อปไปค้นใน Word, เกณฑ์, วิธีแก้ทีละขั้น และค่าที่ถูกต้อง
- [ ] ส่งไฟล์รายงาน + checklist ให้นักศึกษาแก้
- [ ] นักศึกษาแก้แล้วส่งกลับ → ตรวจซ้ำ STEP 1–5 จนไม่มี critical/major และยืนยัน checklist ครบทั้งสองด้าน

---

## เกณฑ์อ้างอิงย่อ — Template Format (ตัวเลขที่ใช้บ่อย)

| รายการ | ค่า |
|---|---|
| กระดาษ | A4 (8.27×11.69") |
| ขอบ (portrait) | บน 1.5 / ล่าง 1.0 / ซ้าย 1.5 / ขวา 1.0 นิ้ว |
| ฟอนต์ (2024) | TH Sarabun New 16pt |
| ฟอนต์ (2023 Times) | Times New Roman 12pt (อังกฤษ) + TH Sarabun 16pt (ไทย) |
| ชื่อบท/หัวข้อส่วนนำ | 18pt หนา กึ่งกลาง |
| ชื่อเรื่องปก / ภาคผนวก | 20pt หนา |
| เยื้องย่อหน้า | 0.80 นิ้ว |
| เลขหน้าส่วนนำ | (1)(2)(3) เริ่มที่บทคัดย่อ |
| เลขหน้าเนื้อหา | 1 2 3 เริ่มที่บทที่ 1 |
| ระยะห่างบรรทัด | 2024 เดี่ยว / 2023 Times = 1.5 |

## เกณฑ์อ้างอิงย่อ — APA 7 (ตัวที่มักผิด)

| รายการ | ไทย (TULIBS) | อังกฤษ (APA ทางการ) |
|---|---|---|
| ชื่อผู้แต่ง | ชื่อ+สกุล ไม่กลับคำ | กลับคำ Last, F. M. |
| ปีพิมพ์ | พ.ศ. | ค.ศ. |
| ผู้แต่ง 2 คน (in-text) | "และ" | "&" |
| ผู้แต่ง 3+ คน (in-text) | "และคณะ" | "et al." |
| เลขหน้า | "น." | "p."/"pp." |
| ช่วงเลขหน้า | en-dash (–) | en-dash (–) |
| DOI | https://doi.org/10.xxxx | https://doi.org/10.xxxx |
| ตัวเอียง | ชื่อวารสาร/หนังสือ | ชื่อวารสาร/หนังสือ |

## ข้อจำกัด (ต้องรู้)
- เลขหน้าสารบัญ/caption ตรวจอัตโนมัติได้เฉพาะจุดที่จับคู่/จำแนกได้; จุดที่สคริปต์แจ้งว่าไม่ครบต้องอ่านยืนยัน
- ห้าม AI render เอกสารเป็นรูปภาพเพื่อดู — ต้องอ่านค่าจริงจากในไฟล์ (python-docx/XML) เท่านั้น
- **อ่านรายงานยังไง**: ช่อง "จุดที่ต้องแก้" คือข้อความจริงในเล่ม — ก๊อปไปวางใน Ctrl+F ของ Word แล้วเคอร์เซอร์ไปถึงจุดนั้นทันที ไม่ต้องไล่นับแผ่น (แถวที่ติดธง ⚠ คือค้นไม่พบ ต้องตรวจซ้ำ)
- **ถ้าใช้ Codex บน macOS แล้วอยากได้เลขแผ่นตรง**: เปิด `python3 scripts/pagemap_service.py worker` ค้างไว้ใน Terminal ปกติหนึ่งหน้าต่างก่อนสั่งตรวจ (crash report ของ soffice ที่เคยเด้ง จะไม่เด้งซ้ำแล้ว สกิลจำไว้ให้)
- แผ่นงานที่มาจาก marker ของ Word จะแสดงด้วย `~` เสมอ และ**คลาดสะสมได้ถึง +8 แผ่นช่วงท้ายเล่ม** (ตรงกับที่เห็นใน Word ตอนบันทึกครั้งล่าสุด) — ไฟล์ที่ไม่เคยบันทึกใน Word จะไม่มีข้อมูลนี้ ให้เปิดใน Word แล้ว Save ทับหนึ่งครั้งก่อนส่งตรวจ
- APA7 ที่ข้อมูลต้นฉบับขาด (ไม่มี DOI/วันที่) → AI ต้องระบุว่าขาด ห้ามแต่งข้อมูลเติม
- สกิลนี้ตรวจ **.docx** เท่านั้น — แนะนำให้ตรวจ Word ก่อนแปลงเป็น PDF (อ่านค่าจริงได้ตรงกว่า)
- **รายงานต้องแยกตาราง Template Format, คำผิด และ APA 7 เสมอ ห้ามรวมกัน**
